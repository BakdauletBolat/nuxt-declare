/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        fontFamily: {
            sans: ['Open Sans', 'sans-serif'],
            serif: ['Open Sans', 'serif'],
        },
        extend: {
            colors: {
                primary: '#CECECE'
            }
        },
    },
    plugins: [],
}
